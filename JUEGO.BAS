1 SCREEN 12
RANDOMIZE TIMER
CLS

DIM CR(440)
DIM SR(440)
DIM BX(100)
DIM BY(100)
DIM BR(100)


'2 PRINT "CUANTOS JUGADORES 1 ,2 ,0 (1/2/0)? "
'KLLL$ = INPUT$(1)
'KLLL = VAL(KLLL$)
'IF (KLLL > 2) OR (KLLL < 0) THEN
'GOTO 2
'END IF

CLS

GEXP = 0
GEXP2 = 0

SX = 0
SY = 0

SX2 = 0
SY2 = 0

X =290
Y =200

X2 =310
Y2 =200

COLO = 3
COLO2 = 12

CLO = 10

CO = 180
CO2 = 360

POD = 10
POD2 = 10

FOR F = 0 TO 440
R = F * 0.0174533
CR(F) = COS(R)
SR(F) = SIN(R)
NEXT

DO
IF X > 640 THEN X = 1
IF Y > 400 THEN Y = 1
IF X < 1 THEN X = 640
IF Y < 1 THEN Y = 400

IF X2 > 640 THEN X2 = 1
IF Y2 > 400 THEN Y2 = 1
IF X2 < 1 THEN X2 = 640
IF Y2 < 1 THEN Y2 = 400

IF CO > 400 THEN CO = 40
IF CO < 40 THEN CO = 400

IF CO2 > 400 THEN CO2 = 40
IF CO2 < 40 THEN CO2 = 400

IF BALL = 60 THEN BALL = 0


XL = (0 * CR(CO)) - (-5 * SR(CO)) + X
YL = (-5 * CR(CO)) - (0 * SR(CO))  + Y
XLL = (0 * CR(CO+30)) - (5 * SR(CO+30)) + X
YLL = (5 * CR(CO+30)) - (0 * SR(CO+30)) + Y
XLLL = (0 * CR(CO-30)) - (5 * SR(CO-30)) + X
YLLL = (5 * CR(CO-30)) - (0 * SR(CO-30)) + Y

XL2 = (0 * CR(CO2)) - (-5 * SR(CO2)) + X2
YL2 = (-5 * CR(CO2)) - (0 * SR(CO2))  + Y2
XLL2 = (0 * CR(CO2+30)) - (5 * SR(CO2+30)) + X2
YLL2 = (5 * CR(CO2+30)) - (0 * SR(CO2+30)) + Y2
XLLL2 = (0 * CR(CO2-30)) - (5 * SR(CO2-30)) + X2
YLLL2 = (5 * CR(CO2-30)) - (0 * SR(CO2-30)) + Y2

LINE (A,B)-(C,D),0
LINE (A,B)-(E,F),0

LINE (A2,B2)-(C2,D2),0
LINE (A2,B2)-(E2,F2),0

LINE (XL,YL)-(XLL,YLL),COLO
LINE (XL,YL)-(XLLL,YLLL),COLO
LINE (XL2,YL2)-(XLL2,YLL2),COLO2
LINE (XL2,YL2)-(XLLL2,YLLL2),COLO2

A = XL
B = YL
C = XLL
D = YLL
E = XLLL
F = YLLL

A2 = XL2
B2 = YL2
C2 = XLL2
D2 = YLL2
E2 = XLLL2
F2 = YLLL2

SELECT CASE INKEY$
CASE CHR$(27)
END

CASE CHR$(0) + CHR$(80)
SY = SY - ((0.3 * CR(CO)) - 0)
SX = SX - (0 - (0.3 * SR(CO)))
CASE CHR$(0) + CHR$(72)
SY = SY + ((0.3 * CR(CO)) - 0)
SX = SX + (0 - (0.3 * SR(CO)))
CASE CHR$(0) + CHR$(75)
CO = CO - 10
CASE CHR$(0) + CHR$(77)
CO = CO + 10
CASE CHR$(32)
BALL = BALL + 1
BX(BALL) = X + (0 - (-10 * SR(CO))) : BY(BALL) = Y + ((-10 * CR(CO)) - 0)
BR(BALL) = CO

CASE CHR$(90)
SY2 = SY2 - ((0.3 * CR(CO2)) - 0)
SX2 = SX2 - (0 - (0.3 * SR(CO2)))
PR1$ = "0"
CASE CHR$(87)
SY2 = SY2 + ((0.3 * CR(CO2)) - 0)
SX2 = SX2 + (0 - (0.3 * SR(CO2)))
PR2$ = "0"
CASE CHR$(65)
CO2 = CO2 - 10
PR3$ = "0"
CASE CHR$(83)
CO2 = CO2 + 10
PR4$ = "0"
CASE CHR$(70)
BALL = BALL + 1
BX(BALL) = X2 + (0 - (-10 * SR(CO2))) : BY(BALL) = Y2 + ((-10 * CR(CO2)) - 0)
BR(BALL) = CO2

END SELECT

IF POD = 0 THEN
FOR CHC = 1 TO 60
BX(CHC) = X + (FIX (RND * 10))
BY(CHC) = Y + (FIX (RND * 10))
BR(CHC) = FIX (RND * 360) + 40
NEXT
CLO = COLO
SX = 0
SY = 0
COLO = 0
GEXP = 100

ELSEIF POD2 = 0 THEN
FOR CHC = 1 TO 60
BX(CHC) = X2 + (FIX (RND * 10))
BY(CHC) = Y2 + (FIX (RND * 10))
BR(CHC) = FIX (RND * 360) + 40
NEXT
CLO = COLO2
SX2 = 0
SY2 = 0
COLO2 = 0
GEXP2 = 100
END IF

Y = Y + SY
X = X + SX

Y2 = Y2 + SY2
X2 = X2 + SX2

FOR FIL = 1 TO 60
IF (BX(FIL) > 640) OR (BX(FIL) < 1) OR (BY(FIL) > 400) OR (BY(FIL) < 1) THEN
 XU = (0 - ( 5 * SR(BR(FIL)))) + BX(FIL)
 YU = (5 * CR(BR(FIL)) - 0) + BY(FIL)
 LINE (BX(FIL),BY(FIL)) - (XU,YU),0
 BX(FIL) = 0
 BY(FIL) = 0
 BR(FIL) = 0
ELSEIF (BX(FIL) <> 0) AND (BY(FIL)<> 0 ) AND (BR(FIL) <> 0) THEN
 XU = (0 - ( 5 * SR(BR(FIL)))) + BX(FIL)
 YU = (5 * CR(BR(FIL)) - 0) + BY(FIL)
 LINE (BX(FIL),BY(FIL)) - (XU,YU),0
 BX(FIL) = BX(FIL) - (0 - (5 * SR(BR(FIL))))
 BY(FIL) = BY(FIL) - (5 * CR(BR(FIL)) - 0)
 XU = (0 - ( 5 * SR(BR(FIL)))) + BX(FIL)
 YU = (5 * CR(BR(FIL)) - 0) + BY(FIL)
 LINE (BX(FIL),BY(FIL)) - (XU,YU),CLO
END IF

 IF (BX(FIL) > (X-4) ) AND (BX(FIL) < (X+4)) AND (BY(FIL) > (Y-4)) AND (BY(FIL) < (Y+4)) THEN
  POD = POD - 1
  BX(FIL) = 650 : BY(FIL) = 450
ELSEIF (BX(FIL) > (X2-4) ) AND (BX(FIL) < (X2+4)) AND (BY(FIL) > (Y2-4)) AND (BY(FIL) < (Y2+4)) THEN
  POD2 = POD2 - 1
  BX(FIL) = 650 : BY(FIL) = 450
  END IF
NEXT

IF GEXP = 1 THEN
CLS
SCREEN 7
COLOR COLO2
LOCATE 12 , 15:PRINT "GANO EL P2 "
LOCATE 13,11 :PRINT "DESEA SEGUIR (S/N)"
YU2$ = INPUT$(1)
IF UCASE$(YU2$) = "N" THEN EXIT LOOP ELSE GOTO 1
EXIT LOOP
ELSEIF GEXP > 0 THEN
GEXP = GEXP - 1
ELSEIF GEXP2 = 1 THEN
CLS
SCREEN 7
COLOR COLO
LOCATE 12 , 15:PRINT "GANO EL P1 "
LOCATE 13,11 :PRINT "DESEA SEGUIR (S/N)"
YU2$ = INPUT$(1)
IF UCASE$(YU2$) = "N" THEN EXIT LOOP ELSE GOTO 1
EXIT LOOP
ELSEIF GEXP2 > 0 THEN
GEXP2 = GEXP2 - 1
END IF

COLOR COLO
LOCATE 1,1: PRINT "P1"
FOR KK = 4 TO (POD+3)
LOCATE 1,KK : PRINT CHR$(178)
NEXT
IF POD >= 0 THEN
LOCATE 1,(POD+4) : PRINT "    "
END IF

COLOR COLO2
LOCATE 1,29: PRINT "P2"
FOR KK = 32 TO (POD2+31)
LOCATE 1,KK : PRINT CHR$(178)
NEXT
IF POD2 >= 0 THEN
LOCATE 1,(POD2+32) : PRINT "       "
END IF

'IF (KLLL = 1) THEN
'IF IJJM = 1 THEN TECLA$ = FN PILOT$(X2,Y2,CHR$(90),CHR$(87),CHR$(65),CHR$(83),CHR$(70),X,Y)
'IF IJJM = 1 THEN IJJM = 0 ELSE IJJM = 1
'IF IJJM = 0 THEN TECLA$ = INKEY$
'ELSEIF (KLLL = 0) THEN
'IF IJJM = 1 THEN TECLA$ = FN PILOT$(X2,Y2,CHR$(90),CHR$(87),CHR$(65),CHR$(83),CHR$(70),X,Y)
'IF IJJM = 0 THEN TECLA$ = FN PILOT$(X2,Y2,CHR$(0) + CHR$(80),CHR$(0) + CHR$(72),CHR$(0) + CHR$(75),CHR$(0) + CHR$(77), CHR$(32), X, Y)
'IF IJJM = 1 THEN IJJM = 0 ELSE IJJM = 1
'END IF
'PAINT (1,1),1
LOOP
CLS
END

'DEF FN PILOT$( X, Y, TE1$, TE2$, TE3$, TE4$, FUE$, XOB, YOB)
'DDX = X - XOB
'DDY = Y - YOB
'IF (ABS(DDY) < 100) AND (ABS(DDX) < 100) THEN
'FN PILOT$ = FUE$
'ELSE
'A = FIX (RND * 4)
'IF A = 1 THEN FN PILOT$ = TE1$
'IF A = 2 THEN FN PILOT$ = TE2$
'IF A = 3 THEN FN PILOT$ = TE3$
'IF A = 4 THEN FN PILOT$ = TE4$
'END IF
'END DEF

SUB KINEA (AB,BC,CD,DE,EF,FG)
X1 = AB * CD / 256
Y1 = BC * CD / 256

END SUB